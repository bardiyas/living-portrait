# Portrait clips

Drop AI-generated MP4 loops here. The manifest at
`lib/portrait/manifest.ts` references these by filename:

- `working.mp4`
- `eating.mp4`
- `idle.mp4`
- `sleeping.mp4`

Per the brief, generation is build-time and hand-curated — runtime is
just static video + the client-side scheduler.
